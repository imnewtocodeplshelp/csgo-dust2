{
  "name": "csgo-dust2-apk",
  "version": "1.0.0",
  "description": "CS:GO Dust2 FPS Multiplayer - Android APK",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed - using www/ directly'",
    "android": "npx cap sync android && npx cap open android",
    "apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
